# 방과후 통합 매니저

학교 방과후학교의 **수강생 · 수익자 부담금 · 방과후 이용권 · 자유수강권 · 정산 · 품의**를
한 프로그램에서 관리합니다. 개인정보를 다루므로 **외부 서버나 클라우드를 쓰지 않고

- 설계 문서: [docs/01-설계안.md](docs/01-설계안.md)
- 지금 상태: **v0.1.2 배포 완료**, v0.1.3 후보 준비 중 (최종 QA)
- 배포 문서: [docs/02-배포.md](docs/02-배포.md)

## 지금 되는 것

| 화면 | 내용 |
|---|---|
| 대시보드 | 전교생 · 지원대상 · 부서 · 수강 수와 준비 순서 |
| 작업공간 | 만들기 · 수정 · 삭제 · 현재 작업공간 선택. **시작일 순 자동 정렬**, 기간 겹침 경고 |
| 학생정보 | 전교생 명단, 학년·반·지원유형 필터, 검색, 정렬, 선택/전체 삭제, Excel. **반은 숫자도 글자도 됩니다**(1, 가, 해) |
| 지원대상자 | 이용권 · 자유수강권 명단, **적용 기간**, 대상학년 불일치 경고, Excel |
| 부서정보 | 부서 · 반 · 강사 · 요일 · 항목별 기준 수강료, **학생별 수정**, **부서금액 반영**, Excel |
| 수강생 명단 | 수강 추가 · 금액 수정 · **취소(최종 징수금액 확정)** · 되돌리기, 부서/학년/반/지원유형/상태 필터, 검색, Excel |
| 학생 상세정보 | 반·번호·이름 하나만으로 조회(학년만 고르면 안내), 20명씩 [더 보기], 지원제도 자격과 적용기간, 작업공간별 수강내역 |
| 변경 이력 | 수강 추가 · 수정 · 취소 · 복원 · 금액 변경 · 부서금액 재반영 기록 |
| 학년도 지원금 설정 | 연간한도 · 지원기간 · 기간별 한도 · 이월/소멸 · 대상학년 · 학생별 예외 · 차감 우선순위 |
| 정산 데이터 생성 | 생성 전 검사, 버튼을 눌렀을 때만 실행, 항목 × 재원 요약, 낡음 표시 |
| 수익자 / 방과후 이용권 / 자유수강권 | 정산 결과 조회 + 학생별 상세 팝업 + Excel. 이용권은 사용액·초과금·기간잔액·연간잔액까지 |
| 품의 양식 받기 | 부서 × 재원 집계, 항목 선택(교재·재료비 통합 포함), 미리보기 후 Excel |
| 학생별 징수 내역 | 학생별 최종 수강료 조회 + 필터 그대로 Excel. **정산과 무관하게** 언제나 볼 수 있습니다 |
| Excel | 업로드 양식 받기 · 업로드(미리보기 검증) · 내려받기 · 오류 목록 내려받기 |
| 백업·복원·업데이트 | 수동/자동 백업, 복원(구버전 자료구조 자동 갱신), 업데이트 확인·설치, 로그 폴더 |

다섯 단계를 모두 마쳤고 v0.1.2까지 배포했습니다. 앱 안 [업데이트 확인]으로
구버전에서 신버전으로 올라가는 것까지 매 릴리스마다 실제로 확인했습니다.

**지원금 사용액과 잔액은 정산이 최신일 때만 표시합니다.** 해당없음 · 정산 전 ·
재정산 필요 · 최신을 분명히 갈라 쓰며, 낡은 정산의 숫자를 최신처럼 보여 주지 않습니다.

## 이 프로그램이 지키는 것

- 부서 기준금액을 고쳐도 **이미 등록된 학생의 금액은 저절로 바뀌지 않습니다.**
  반영은 사람이 [부서금액 반영]을 눌렀을 때만 일어나고, 그때도 무엇이 어떻게 바뀌는지
  먼저 보여 줍니다. 학생별로 고쳐 둔 금액은 기본 방식에서 그대로 지켜집니다.
- **수강 취소는 자료를 지우지 않습니다.** 상태만 취소로 바꾸고 사유를 받습니다.
  되돌릴 수 있고, 변경 이력에 남습니다.
- 작업공간 순서는 **시작일 하나**로 정해집니다. 화면에 보이는 차례가 곧 지원금
  누적 계산의 차례입니다.
- **수강취소가 전액 0원이 아닙니다.** 중도에 그만두어도 이미 발생한 비용은
  징수합니다. 취소할 때 항목별 최종 징수금액을 확인·수정하고, 그 금액이 정산에
  들어갑니다. 전액 면제한 취소만 정산에서 빠집니다.
- **사용자의 한/영 상태를 앱이 바꾸지 않습니다.** 한글을 조합하는 중에는
  미완성 글자로 검색하지 않고, 글자가 완성된 뒤에 찾습니다.

## 개발

```bash
npm install
npm run app        # 개발 중 실행 (창이 뜹니다)
npm run check      # 타입 검사 + 화면 시험 + Rust 시험
npm run app:build  # 설치 파일 만들기
```

| 명령 | 하는 일 |
|---|---|
| `npm run dev` | 화면만 브라우저에서 (Tauri 명령은 동작하지 않습니다) |
| `npm run build` | 타입 검사 + 화면 번들 |
| `npm run test` | 화면 쪽 시험 (vitest) |
| `npm run check` | `tsc --noEmit` + vitest + `cargo test` |
| `npm run version:set 0.2.0` | 버전 세 곳을 한꺼번에 맞춤 |

## 구조

```
src/                      화면 (React + TypeScript)
  components/             공용 UI — 표 · 팝업 · 토스트 · Excel 도구 · 수강 팝업
  pages/                  화면별 구현
  ipc/                    Rust 명령 호출 (api.ts) 와 타입 (types.ts)
  lib/                    포맷터 · 전역 상태
  styles/                 디자인 토큰과 공용 스타일
src-tauri/
  migrations/00N_*.sql    스키마 원본 — 배포된 파일은 고치지 않는다
  src/db/                 연결 · 마이그레이션
  src/repo/               SQL
  src/domain/             업무 규칙 (DB 의존 없음, 테스트 대상)
  src/excel/              읽기(calamine) · 쓰기(rust_xlsxwriter)
  src/commands/           IPC 껍데기 (로직 없음)
```

## 자료가 저장되는 곳

| 대상 | 위치 |
|---|---|
| 업무 DB | `%APPDATA%\kr.school.afterschool\afterschool.db` |
| 백업 | 위 폴더의 `backups\` |
| Excel 출력 | 위 폴더의 `exports\` |
| 로그 | 위 폴더의 `logs\` (30일 보관, 개인정보 없음) |

실행 파일과 자료를 나누어 두었으므로 **프로그램을 업데이트해도 자료는 그대로**입니다.
자료구조가 바뀌는 업데이트에서는 적용 직전에 `backups\`로 자동 백업합니다
(파일 복사가 아니라 SQLite 백업 API를 씁니다).

## 남은 일

- [x] 앱 아이콘 교체 (2026-09-06) — 원본은 `src-tauri/icons/source.png`
- [x] GitHub 저장소 `lohemal/AfterSchool-Total-Manager` 만들고 `main` 푸시
- [x] 서명 키 Secret `TAURI_SIGNING_PRIVATE_KEY` 등록
      (비밀번호 Secret은 등록하지 않는다 — `docs/02-배포.md` 5장)
- [x] 첫 Release 발행 (2026-09-06) — v0.1.0, v0.1.1
- [x] 실제 구버전 → 신버전 업데이트 시험 통과 —
      v0.1.0 앱에서 [업데이트 확인] → v0.1.1 설치 → 자료 그대로

Phase 5는 여기서 끝났습니다. 지금은 최종 QA 중입니다.

### 최종 QA 에서 고친 것

**v0.1.3 후보**

- [x] **취소자 최종 징수금액 정산** — 중도 취소해도 이미 발생한 비용은 징수합니다.
      `status` 가 아니라 charge 금액이 정산 대상을 정합니다 (설계안 22장).
- [x] **행정자료 › 학생별 징수 내역** — 정산과 무관하게 보는 조회·Excel (설계안 23장)
- [x] **텍스트 입력·한글 조합 UX** — 한/영을 강제하지 않고, 조합 중에는 검색하지
      않습니다 (설계안 24장)

**v0.1.2 (배포됨)**

- [x] **학생 상세정보 조회 범위** — 학년만 고르면 안내, 20명씩 [더 보기] (설계안 21장)
- [x] **반을 문자로** — `가 나 다`, `해 달 별` 같은 반 이름을 쓸 수 있게 했습니다.
      기존 숫자 반은 뜻이 그대로입니다 (`1` → `'1'`). 자료구조 004.
      자세한 내용은 설계안 20장.

## 배포

| 항목 | 값 |
|---|---|
| 저장소 | `lohemal/AfterSchool-Total-Manager` |
| 태그 | `v0.1.0`, `v0.1.1`, … (`v` + 앱 버전) |
| 첨부 파일 | `afterschool_<버전>_x64-setup.exe` · `.exe.sig` · `latest.json` · `SHA256SUMS.txt` |
| 앱 표시명 | 방과후 통합 매니저 (한글 그대로) |

설치파일 이름만 영문입니다 — GitHub Release가 첨부 파일 이름에서 한글을 떼어 내
`latest.json`의 주소와 어긋나기 때문입니다. 자세한 내용은 `docs/02-배포.md` 6장.
